[project]
name = "v0-1-0-beta"
version = "0.1.0"
description = "NOVUM Fresh-line - a next-generation modular Discord Bot."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "discord-py>=2.7.1",
    "logging>=0.4.9.6",
    "python-dotenv>=1.2.2",
]

[tool.uv.workspace]
members = [
    "dotenv",
]
